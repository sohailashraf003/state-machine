#!/usr/bin/env bash
# This variable contains absolute path of this `setup` script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
#Run test and package the jar 
mvn -f $DIR/../pom.xml clean package


